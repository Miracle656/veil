# Copy to .env.local. Testnet defaults work out of the box.

# Stellar network (testnet | mainnet)
NEXT_PUBLIC_NETWORK=testnet

# Base URL of the x402 resource server (examples/x402-api/server)
NEXT_PUBLIC_API_URL=http://localhost:4021
