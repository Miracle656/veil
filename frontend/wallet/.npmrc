# @sentry/nextjs@^8 declares a conservative peer range (next ^13–15) that does not
# yet list Next 16, though it runs fine against it. Allow the install to proceed so
# `npm ci` (CI + wallet-e2e workflow) can resolve the tree deterministically.
legacy-peer-deps=true
