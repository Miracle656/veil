//! Uniffi helpers that exist only for the binding layer.

/// Reserved for future uniffi callback interfaces (progress reporting from
/// the prover into JS). Kept as a named placeholder so the scaffolding file
/// stays structurally complete.
pub const UNIFFI_META_NAMESPACE: &str = "spp_native";
